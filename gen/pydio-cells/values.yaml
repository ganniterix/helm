base-chart:
  nameOverride: pydio-cells
  fullnameOverride: ""

  serviceAccount:
    # Specifies whether a service account should be created
    create: true
    # Annotations to add to the service account
    annotations: {}
    # The name of the service account to use.
    # If not set and create is true, a name is generated using the fullname template
    name:

  service:
    type: ClusterIP
    ports: 
      - port: 8080
        targetPort: 8080
        name: p8080-tcp
        protocol: TCP
      - port: 33060
        targetPort: 33060
        name: p33060-tcp
        protocol: TCP

  ingresses: 
    - name: pydio-cells
      # disabled: true
      annotations:
        spec.ingressClassName: nginx
        cert-manager.io/issuer: letsencrypt-staging
        cert-manager.io/issuer-kind: ClusterIssuer
      spec:
        ingressClassName: nginx
        tls:
          - hosts:
              - pydio-cells.e-kavallieri.com
            secretName: pydio-cells-certsecret
        rules:
          - path: '/'
            serviceName: p33060-tcp
            servicePort: 33060
            host: pydio-cells.e-kavallieri.com
            pathType: Prefix

  statefulset:
    podManagementPolicy: OrderedReady
    replicas: 1
    revisionHistoryLimit: 10
    updateStrategy:
      rollingUpdate:
        partition: 0
      type: RollingUpdate
    volumes:
      - name: appdata
        nfs: 
          server: 192.168.1.10
          path: /appdata
    sharedEnv: 
      - name: GUID
        value: "1000"
      - name: PUID
        value: "1000"
      - name: TZ
        value: Etc/UTC
    sharedValues:
      imagePullPolicy: IfNotPresent
      securityContext: {}
      resources: {}
    spec:
      imagePullSecrets: []
      nodeSelector: {}
      tolerations: []
      affinity: {}
    containers: 
      - name: pydio
        image: lscr.io/linuxserver/pydio-cells:latest
        env: 
          - name: EXTERNALURL
            value: yourdomain.url
        ports: 
          - containerPort: 8080
            name: p8080-tcp
            protocol: TCP
          - containerPort: 33060 #optional
            name: p33060 #optional-tcp
            protocol: TCP
        volumeMounts: []
    volumeClaimTemplates: 
      - volumeClaimTemplate:
          metadata:
            name: pvc-config
          spec:
            storageClassName: ceph-block
            accessModes: 
              - ReadWriteOnce
            resources:
              requests:
                storage: 20Gi
        volumeMount:
          mountPath: /config

  static_volumes: []
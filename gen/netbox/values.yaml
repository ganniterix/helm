base-chart:
  nameOverride: netbox
  fullnameOverride: ""

  serviceAccount:
    # Specifies whether a service account should be created
    create: true
    # Annotations to add to the service account
    annotations: {}
    # The name of the service account to use.
    # If not set and create is true, a name is generated using the fullname template
    name:

  service:
    type: ClusterIP
    ports: 
      - port: 8000
        targetPort: 8000
        name: p8000-tcp
        protocol: TCP

  ingresses: 
    - name: netbox
      # disabled: true
      annotations:
        spec.ingressClassName: nginx
        cert-manager.io/issuer: letsencrypt-staging
        cert-manager.io/issuer-kind: ClusterIssuer
      spec:
        ingressClassName: nginx
        tls:
          - hosts:
              - netbox.e-kavallieri.com
            secretName: netbox-certsecret
        rules:
          - path: '/'
            serviceName: p8000-tcp
            servicePort: 8000
            host: netbox.e-kavallieri.com
            pathType: Prefix

  statefulset:
    podManagementPolicy: OrderedReady
    replicas: 1
    revisionHistoryLimit: 10
    updateStrategy:
      rollingUpdate:
        partition: 0
      type: RollingUpdate
    volumes:
      - name: appdata
        nfs: 
          server: 192.168.1.10
          path: /appdata
    sharedEnv: 
      - name: GUID
        value: "1000"
      - name: PUID
        value: "1000"
      - name: TZ
        value: Etc/UTC
    sharedValues:
      imagePullPolicy: IfNotPresent
      securityContext: {}
      resources: {}
    spec:
      imagePullSecrets: []
      nodeSelector: {}
      tolerations: []
      affinity: {}
    containers: 
      - name: netbox
        image: lscr.io/linuxserver/netbox:latest
        env: 
          - name: SUPERUSER_EMAIL
            value: 
          - name: SUPERUSER_PASSWORD
            value: 
          - name: ALLOWED_HOST
            value: 
          - name: DB_NAME
            value: 
          - name: DB_USER
            value: 
          - name: DB_PASSWORD
            value: 
          - name: DB_HOST
            value: 
          - name: DB_PORT
            value: 
          - name: REDIS_HOST
            value: 
          - name: REDIS_PORT
            value: 
          - name: REDIS_PASSWORD
            value: 
          - name: REDIS_DB_TASK
            value: 
          - name: REDIS_DB_CACHE
            value: 
        ports: 
          - containerPort: 8000
            name: p8000-tcp
            protocol: TCP
        volumeMounts: []
    volumeClaimTemplates: 
      - volumeClaimTemplate:
          metadata:
            name: pvc-config
          spec:
            storageClassName: ceph-block
            accessModes: 
              - ReadWriteOnce
            resources:
              requests:
                storage: 20Gi
        volumeMount:
          mountPath: /config

  static_volumes: []
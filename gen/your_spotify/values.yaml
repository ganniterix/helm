base-chart:
  nameOverride: your_spotify
  fullnameOverride: ""

  serviceAccount:
    # Specifies whether a service account should be created
    create: true
    # Annotations to add to the service account
    annotations: {}
    # The name of the service account to use.
    # If not set and create is true, a name is generated using the fullname template
    name:

  service:
    type: ClusterIP
    ports: 
      - port: 80
        targetPort: 80
        name: p80-tcp
        protocol: TCP
      - port: 443
        targetPort: 443
        name: p443-tcp
        protocol: TCP

  ingresses: 
    - name: your_spotify
      # disabled: true
      annotations:
        spec.ingressClassName: nginx
        cert-manager.io/issuer: letsencrypt-staging
        cert-manager.io/issuer-kind: ClusterIssuer
      spec:
        ingressClassName: nginx
        tls:
          - hosts:
              - your_spotify.e-kavallieri.com
            secretName: your_spotify-certsecret
        rules:
          - path: '/'
            serviceName: your_spotify
            servicePort: 80
            host: your_spotify.e-kavallieri.com
            pathType: Prefix

  statefulset:
    podManagementPolicy: OrderedReady
    replicas: 1
    revisionHistoryLimit: 10
    updateStrategy:
      rollingUpdate:
        partition: 0
      type: RollingUpdate
    volumes:
      - name: appdata
        nfs: 
          server: 192.168.1.10
          path: /appdata
    sharedEnv: 
      - name: GUID
        value: "1000"
      - name: PUID
        value: "1000"
      - name: TZ
        value: Etc/UTC
    sharedValues:
      imagePullPolicy: IfNotPresent
      securityContext: {}
      resources: {}
    spec:
      imagePullSecrets: []
      nodeSelector: {}
      tolerations: []
      affinity: {}
    containers: 
      - name: your_spotify
        image: lscr.io/linuxserver/your_spotify:latest
        env: 
          - name: APP_URL
            value: http://localhost
          - name: SPOTIFY_PUBLIC
            value: 
          - name: SPOTIFY_SECRET
            value: 
          - name: CORS
            value: http://localhost:80,https://localhost:443
          - name: MONGO_ENDPOINT
            value: mongodb://mongo:27017/your_spotify
        ports: 
          - containerPort: 80
            name: p80-tcp
            protocol: TCP
          - containerPort: 443
            name: p443-tcp
            protocol: TCP
        volumeMounts: []
    volumeClaimTemplates: 
      - volumeClaimTemplate:
          metadata:
            name: pvc-config
          spec:
            storageClassName: ceph-block
            accessModes: 
              - ReadWriteOnce
            resources:
              requests:
                storage: 20Gi
        volumeMount:
          mountPath: /config

  static_volumes: []